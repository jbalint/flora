:- use_argumentation_theory{ogcle}.
?- [atck1alt>>ogcle].

@{r} p.
@{r} w :- p.
_opposes(p,w).

/*
    w is defeated because it is notBeaterFor(binaryExclusion(${w},${p}))
*/
%pretest :- naf w, writeln(passed)@_plg.

%test :- tell(temp)@_plg(),
	%pretest,
	told@_prolog.

