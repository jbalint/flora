function ?x+?y:=?z if ?z is ?x+?y.
function plus2(?x):=?x+2.
function inc(?x):=?y if ?y=?x+1.

%test1 :-
	writeln(inc(4))@_plg,
	writeln(inc(plus2(7)))@_plg.

%test :-
	tell(temp)@_plg,
	%test1,
	told@_plg.
