/*
  ATCO: The client, like SILK, should generate a flora-addable file containing
        SILK refutablebodylit facts. The file should look like this:

	#include "atco.flh"
	SILK_REFUTABLEBODYLIT(123,Head1,_@,${aaa(pp)}).
	SILK_REFUTABLEBODYLIT(567,Head2,_@,${naf aaa(pp)}).
    	... ... ...
        %%SILK_IRREFUTABLEBODYLIT(567,Head3,_@,${naf ccc(pp)}).
        ... ... ...

        So, if the main KB is foo.flr then it should generate something like
	foo_atco.flr with the above statements. The whole thing is loaded
	like this:

	?- [atco_init, +foo_atco].

        Then load foo.flr either statically or dynamically (using _add).
*/

:- compiler_options([xpp_on]).

#include "atco.flh"

?- dynamic SILK_REFUTABLEBODYLIT/4,
           %%SILK_IRREFUTABLEBODYLIT/4,
	   SILK_BODYINFO/6.

?- index(SILK_REFUTABLEBODYLIT/4, [1,2]),
    %%index(SILK_IRREFUTABLEBODYLIT/3, [1,2]),
    index(SILK_BODYINFO/6, [1,2]).

?- flrprolog:flora_define_permanent_prolog(SILK_REFUTABLEBODYLIT,4),
   %%flrprolog:flora_define_permanent_prolog(SILK_IRREFUTABLEBODYLIT,4),
   flrprolog:flora_define_permanent_prolog(SILK_BODYINFO,6),

?- flrprolog:flora_define_permanent_prolog(SILK_ATCO_REFUTABLE_WRAP,5).
