//?- flora_set_unification_mode(correct)@_plg(flrerrhandler).
//?- flora_set_unification_mode(fast)@_plg(flrerrhandler).

?- [+'silk-predicates'],
	_production(on),
	[+'silk-predicates_at_builtin'>>builtin],
	_production(on),
	[+'process-core'],
	_production(on),
	[+'process-core-queries'],
	_production(on),
	[+'process-doc'].

//?- _trace(log).

//?- when(?X,?Y)(?e1,?e2) .
?- profile_call(${when(?X,?Y)(?e1,?e2)})@_plg(xsb_profiling).
