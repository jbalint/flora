:- use_argumentation_theory.

:- setsemantics{tabling(incremental)}.


@{r} p.
@{r} w :- p.
\opposes(p,w).

%pretest :- w, writeln(passed)@\plg.

%test :- tell(temp)@\plg(),
	%pretest,
	told@\prolog.

