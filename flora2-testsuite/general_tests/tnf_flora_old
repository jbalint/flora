Parse error
Parse error
test(Fact3), neg aaaaa, neg obj[ppp], neg obj[?X], neg obj[?Y], neg pppppp
Parse error
Parse error
Parse error
Parse error
Parse error
Parse error
Parse error
Parse error
Parse error
Parse error
Parse error
test(Fact16), neg pp, neg p, neg qq, neg q, r, rr
Parse error

?-(test(LT1), (naf exists(?X,?Y)^((naf neg aaaaa, naf neg obj[(ppp, (?X, ?Y))])), exists(?X)^(neg pppppp(?X))))
?-(test(LT2), ((neg aaaaa; neg obj[(ppp, (?X, ?Y))]), neg pppppp))
?-(test(LT3), (neg aaaaa, (neg obj[(ppp; (?X; ?Y))], neg pppppp)))
?-(test(LT4), (exists(?X,?Y)^((aaaaa, obj[(ppp, (?X, ?Y))])); naf exists(?X)^(naf pppppp(?X))))
?-(test(LT5), (exists(?X,?Y)^((neg aaaaa, neg obj[(ppp, (?X, ?Y))])); naf exists(?X)^(naf pppppp(?X))))
?-(test(LT6), (naf exists(?X,?Y)^((naf neg aaaaa, naf neg obj[(ppp, (?X, ?Y))])), exists(?X)^(neg pppppp(?X))))
?-(test(LT7), (naf exists(?X,?Y)^((naf neg aaaaa, naf neg obj[(ppp, (?X, ?Y))])), naf exists(?X)^(naf pppppp(?X))))
?-(test(LT8), (naf exists(?X,?Y)^((naf neg aaaaa, naf obj[(ppp, (?X, ?Y))])), naf exists(?X)^(naf pppppp(?X))))
?-(test(LT9), naf exists(?X,?Y)^(naf neg obj(ppp, ?X, ?Y)))
?-(test(LT10), exists(?X,?Y)^(neg obj[p]))
?-(test(LT12), exists(?X,?Y)^(neg obj[((p, q->kkk); r)]))
?-(test(LT13), exists(?X,?Y)^(neg obj[((p, neg q->kkk); r)]))
?-(test(LT14), neg obj[((p->kkk, q); r)])
?-(test(LT15), ((neg pp; neg p), ((neg qq; neg q); (r; rr))))
?-(test(LT16), ((neg pp, neg p), ((neg qq, neg q), (r, rr))))
?-(test(LT17), ((pp, p); ((qq, q), (neg r, neg rr))))



Parse error
Parse error
?-(test(insert3), insert{insertrule_z{(neg obj(p), neg obj(r) :- true)}})
?-(test(insert4), insertrule{(neg obj(p), neg obj(r) :- true)})
Parse error
Parse error
?-(test(insert7), insert{neg a[p], neg a[q]})
?-(test(insert8), insert{a[p], a[q]})
Parse error
Parse error
Parse error
?-(test(insert12), insertrule{(neg obj[p->kkk], neg obj[q], neg obj[r] :- true)})
?-(test(insert13), insert{insertrule_z{(neg obj[p->kkk], neg obj[q], neg obj[r] :- true)}})
Parse error
Parse error
Parse error
Parse error
Parse error
Parse error
Parse error
Parse error
Parse error
?-(test(insert23), insertrule{(p123[ac->1], p123[bc->1] :- abc)})
?-(test(insert24), insert{p234(ac), p234(bc)})
Parse error

test(Rule1)
Parse error
test(Rule2)
Parse error
test(Rule3)
neg aaaaa, neg pppppp :- true
test(Rule4)
neg obj[ppp], neg obj[?X], neg obj[?Y], neg pppppp :- true
test(Rule5)
Parse error
test(Rule6)
Parse error
test(Rule7)
Parse error
test(Rule8)
Parse error
test(Rule9)
Parse error
test(Rule10)
Parse error
test(Rule11)
Parse error
test(Rule12)
Parse error
test(Rule13)
Parse error
test(Rule14)
obj[p->kkk], obj[q], neg obj[r] :- true
test(Rule15)
Parse error
test(Rule16)
Parse error

Parse error
Parse error
Parse error
Parse error
Parse error
Parse error
Parse error
Parse error
Parse error
Parse error
test(NestedFrame_fact11), a[|b{0..*}=>x|], b:dd, a[|b{0..*}=>y|], b:dd
Parse error

Parse error
Parse error
Parse error
Parse error
Parse error

Parse error
Parse error
Parse error
Parse error
?-(test(NestedFrame_LT1), neg a[(b; c->u[(d; e)])])
?-(test(NestedFrame_LT2), neg a[(b; c->u[(d, e)])])



++Error[Flora-2]> [tnf_data.flr] <Parser> near line(2)/char(27) `exist'
                no quantifiers are allowed in rule head

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(3)/char(14) `neg'
                only conjunction of literals (or equivalent) is allowed in rule heads and facts in this context

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(5)/char(23) `exist'
                no quantifiers are allowed in rule head

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(6)/char(23) `exist'
                no quantifiers are allowed in rule head

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(7)/char(19) `exist'
                no quantifiers are allowed in rule head

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(8)/char(19) `exist'
                no quantifiers are allowed in rule head

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(9)/char(19) `exist'
                no quantifiers are allowed in rule head

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(10)/char(18) `exist'
                no quantifiers are allowed in rule head

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(11)/char(19) `forall'
                no quantifiers are allowed in rule head

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(12)/char(19) `forall'
                no quantifiers are allowed in rule head

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(13)/char(19) `forall'
                no quantifiers are allowed in rule head

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(14)/char(15) `neg'
                only conjunction of literals (or equivalent) is allowed in rule heads and facts in this context

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(15)/char(15) `neg'
                only conjunction of literals (or equivalent) is allowed in rule heads and facts in this context

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(17)/char(15) `neg'
                only conjunction of literals (or equivalent) is allowed in rule heads and facts in this context

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(40)/char(32) `;'
                only conjunction of literals (or equivalent) is allowed in rule heads and facts in this context

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(41)/char(36) `;'
                only conjunction of literals (or equivalent) is allowed in rule heads and facts in this context

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(44)/char(31) `neg'
                only conjunction of literals (or equivalent) is allowed in rule heads and facts in this context

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(45)/char(27) `['
                only conjunction of literals (or equivalent) is allowed in rule heads and facts in this context

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(48)/char(26) `neg'
                only conjunction of literals (or equivalent) is allowed in rule heads and facts in this context

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(49)/char(31) `neg'
                only conjunction of literals (or equivalent) is allowed in rule heads and facts in this context

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(50)/char(31) `neg'
                only conjunction of literals (or equivalent) is allowed in rule heads and facts in this context

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(53)/char(34) `['
                only conjunction of literals (or equivalent) is allowed in rule heads and facts in this context

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(54)/char(31) `forall'
                no logical quantifiers are allowed here

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(55)/char(35) `forall'
                no quantifiers are allowed in rule head

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(56)/char(31) `exist'
                no logical quantifiers are allowed here

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(57)/char(32) `exist'
                no logical quantifiers are allowed here

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(58)/char(36) `exist'
                no quantifiers are allowed in rule head

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(59)/char(27) `neg'
                only conjunction of literals (or equivalent) is allowed in rule heads and facts in this context

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(60)/char(27) `neg'
                only conjunction of literals (or equivalent) is allowed in rule heads and facts in this context

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(61)/char(31) `neg'
                only conjunction of literals (or equivalent) is allowed in rule heads and facts in this context

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(64)/char(35) `['
                only conjunction of literals (or equivalent) is allowed in rule heads and facts in this context

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(68)/char(14) `exist'
                no quantifiers are allowed in rule head

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(70)/char(1) `neg'
                only conjunction of literals (or equivalent) is allowed in rule heads and facts in this context

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(76)/char(10) `exist'
                no quantifiers are allowed in rule head

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(78)/char(10) `exist'
                no quantifiers are allowed in rule head

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(80)/char(6) `exist'
                no quantifiers are allowed in rule head

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(82)/char(6) `exist'
                no quantifiers are allowed in rule head

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(84)/char(6) `exist'
                no quantifiers are allowed in rule head

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(86)/char(5) `exist'
                no quantifiers are allowed in rule head

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(88)/char(5) `forall'
                no quantifiers are allowed in rule head

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(90)/char(5) `forall'
                no quantifiers are allowed in rule head

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(92)/char(1) `neg'
                only conjunction of literals (or equivalent) is allowed in rule heads and facts in this context

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(96)/char(1) `neg'
                only conjunction of literals (or equivalent) is allowed in rule heads and facts in this context

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(98)/char(1) `neg'
                only conjunction of literals (or equivalent) is allowed in rule heads and facts in this context

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(101)/char(26) `neg'
                only conjunction of literals (or equivalent) is allowed in rule heads and facts in this context

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(102)/char(26) `neg'
                only conjunction of literals (or equivalent) is allowed in rule heads and facts in this context

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(103)/char(26) `neg'
                only conjunction of literals (or equivalent) is allowed in rule heads and facts in this context

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(104)/char(26) `neg'
                only conjunction of literals (or equivalent) is allowed in rule heads and facts in this context

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(105)/char(26) `neg'
                only conjunction of literals (or equivalent) is allowed in rule heads and facts in this context

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(106)/char(26) `neg'
                only conjunction of literals (or equivalent) is allowed in rule heads and facts in this context

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(107)/char(26) `neg'
                only conjunction of literals (or equivalent) is allowed in rule heads and facts in this context

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(108)/char(26) `neg'
                only conjunction of literals (or equivalent) is allowed in rule heads and facts in this context

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(109)/char(26) `neg'
                only conjunction of literals (or equivalent) is allowed in rule heads and facts in this context

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(110)/char(27) `neg'
                only conjunction of literals (or equivalent) is allowed in rule heads and facts in this context

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(113)/char(27) `neg'
                only conjunction of literals (or equivalent) is allowed in rule heads and facts in this context

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(115)/char(29) `%bb'
                unexpected transactional symbol

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(116)/char(29) `%bb'
                unexpected transactional symbol

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(117)/char(41) `[|'
                invalid boolean signature

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(118)/char(27) `neg'
                only conjunction of literals (or equivalent) is allowed in rule heads and facts in this context

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(119)/char(27) `neg'
                only conjunction of literals (or equivalent) is allowed in rule heads and facts in this context

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(121)/char(38) `neg'
                only conjunction of literals (or equivalent) is allowed in rule heads and facts in this context

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(122)/char(38) `neg'
                only conjunction of literals (or equivalent) is allowed in rule heads and facts in this context

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(123)/char(42) `neg'
                only conjunction of literals (or equivalent) is allowed in rule heads and facts in this context

++1 error

++compilation aborted


++Error[Flora-2]> [tnf_data.flr] <Parser> near line(124)/char(42) `neg'
                only conjunction of literals (or equivalent) is allowed in rule heads and facts in this context

++1 error

++compilation aborted

